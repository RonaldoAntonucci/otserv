# otserv

Open Tibia server project based on The Forgotten Server and OTClient.

The project uses Docker for local development on macOS and native services for deployment on the Ubuntu VPS.

Implementation is tracked in [`.specs/features/bootstrap-open-tibia-server`](.specs/features/bootstrap-open-tibia-server).
